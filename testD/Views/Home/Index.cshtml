@{
    ViewBag.Title = "Home Page";
}

@section Scripts
{
   <script>
$(document).ready(function(){
  // Initialize Tooltip
  $('[data-toggle="tooltip"]').tooltip();

  // Add smooth scrolling to all links in navbar + footer link
  $(".navbar a, footer a[href='#myPage']").on('click', function(event) {

    // Make sure this.hash has a value before overriding default behavior
    if (this.hash !== "") {

      // Prevent default anchor click behavior
      event.preventDefault();

      // Store hash
      var hash = this.hash;

      // Using jQuery's animate() method to add smooth page scroll
      // The optional number (900) specifies the number of milliseconds it takes to scroll to the specified area
      $('html, body').animate({
        scrollTop: $(hash).offset().top
      }, 900, function(){

        // Add hash (#) to URL when done scrolling (default click behavior)
        window.location.hash = hash;
      });
    } // End if
  });
})
</script>

<script>
$(document).ready(function(){
  // Initialize Tooltip
  $('[data-toggle="tooltip"]').tooltip();


  // Add smooth scrolling to Intro's Links
  $("#intro a").on('click', function (event) {

    // Make sure this.hash has a value before overriding default behavior
    if (this.hash !== "") {

      // Prevent default anchor click behavior
      event.preventDefault();

      // Store hash
      var hash = this.hash;

      // Using jQuery's animate() method to add smooth page scroll
      // The optional number (900) specifies the number of milliseconds it takes to scroll to the specified area
      $('html, body').animate({
        scrollTop: $(hash).offset().top
      }, 900, function(){

        // Add hash (#) to URL when done scrolling (default click behavior)
        window.location.hash = hash;
      });
    } // End if
  });
})
</script>
}


<div id="myCarousel" class="carousel slide" data-ride="carousel">
    <!-- Indicators -->
    <ol class="carousel-indicators">
        <li data-target="#myCarousel" data-slide-to="0" class="active"></li>
        <li data-target="#myCarousel" data-slide-to="1"></li>
        <li data-target="#myCarousel" data-slide-to="2"></li>
    </ol>

    <!-- Wrapper for slides -->
    <div class="carousel-inner" role="listbox">
        <div class="item active">
            <img src="~/Image/nikea.jpg" alt="New York" width="1200" height="700">
            <div class="carousel-caption">
                <h3>Black Blade</h3>
                <p>The best style for the best runner.</p>
            </div>
        </div>

        <div class="item">
            <img src="~/Image/nikeb.jpg" alt="Chicago" width="1200" height="700">
            <div class="carousel-caption">
                <h3>Iron Wind</h3>
                <p>Goodbye, Evil - To be the Man like Iron.</p>
            </div>
        </div>

        <div class="item">
            <img src="~/Image/nike_c.png" alt="Los Angeles" width="1200" height="700">
            <div class="carousel-caption">
                <h3>Wind of Sky </h3>
                <p>Take a deep Breath, and play with wind.</p>
            </div>
        </div>
    </div>

    <!-- Left and right controls -->
    <a class="left carousel-control" href="#myCarousel" role="button" data-slide="prev">
        <span class="glyphicon glyphicon-chevron-left" aria-hidden="true"></span>
        <span class="sr-only">Previous</span>
    </a>
    <a class="right carousel-control" href="#myCarousel" role="button" data-slide="next">
        <span class="glyphicon glyphicon-chevron-right" aria-hidden="true"></span>
        <span class="sr-only">Next</span>
    </a>
</div>

<!-- Container  -->
<div id="intro" class="container text-center">
    <h3>SNEAKER MAN</h3>
    <p><em>We love Sneaker!</em></p>
    <p>The worlds leading luxury sneaker and streetwear store built for the digital age, Sneaker Man merges the efficiency of the web with the tangibility of the physical experience. Stocking the worlds leading international fashion brands including Alexander Mcqueen, Balenciaga, Buscemi, Valentino and Saint Laurent alongside the most exclusive and sought after sneakers from the worlds leading sports lifestyle brands Nike, Adidas, Reebok and Puma.</p>
    <br>
    <div class="row">
        <div class="col-sm-4">
            <p class="text-center">
                <a href="#whoweare">
                    <div style="height:100%;width:100%" class="btn btn-default">
                        <strong>Who we are</strong>
                    </div>
                </a> </p><br>
            
        </div>
        <div class="col-sm-4">
            <p class="text-center">
                <a href="#whatwehave">
                    <div style="height:100%;width:100%" class="btn btn-default">
                        <strong>What we have</strong>
                    </div>
                </a></p>
            <br>

        </div>
        <div class="col-sm-4">
            <p class="text-center">
                <a href="#ourvision" >
                    <div style="height:100%;width:100%" class="btn btn-default">
                        <strong>Our vision</strong>
                    </div>
                </a>
            </p><br>
        </div>
    </div>
</div>


<!-- Container (TOUR Section) -->
<div id="whoweare" class="customize-background1">
   
    <h3>WHO WE ARE</h3>
    <p>The worlds first luxury sneaker and streetwear store built for the digital age, 
    SneakerMan® merges the efficiency of the Internet with the tangibility of the physical experience. </p>
    
</div>


<!-- Container (TOUR Section) -->
<div id="whatwehave" class="customize-background2">
   
    <h3>WHAT WE HAVE</h3>
    <p>athletic shoes, tennis shoes, gym shoes, runners, takkies, or trainers.</p>

</div>

<!-- Container (TOUR Section) -->
<div id="ourvision" class="customize-background3">
   
    <h3>OUR VISION</h3>
    <p>SneakerMan’s vision of luxury commerce is that exists almost like an art gallery.</p>

</div>


<script>
$(document).ready(function(){
  // Initialize Tooltip
  $('[data-toggle="tooltip"]').tooltip();

  // Add smooth scrolling to all links in navbar + footer link
  $(".navbar a, footer a[href='#myPage']").on('click', function(event) {

    // Make sure this.hash has a value before overriding default behavior
    if (this.hash !== "") {

      // Prevent default anchor click behavior
      event.preventDefault();

      // Store hash
      var hash = this.hash;

      // Using jQuery's animate() method to add smooth page scroll
      // The optional number (900) specifies the number of milliseconds it takes to scroll to the specified area
      $('html, body').animate({
        scrollTop: $(hash).offset().top
      }, 900, function(){

        // Add hash (#) to URL when done scrolling (default click behavior)
        window.location.hash = hash;
      });
    } // End if
  });
})
</script>

<script>
$(document).ready(function(){
  // Initialize Tooltip
  $('[data-toggle="tooltip"]').tooltip();


  // Add smooth scrolling to Intro's Links
  $("#intro a").on('click', function (event) {

    // Make sure this.hash has a value before overriding default behavior
    if (this.hash !== "") {

      // Prevent default anchor click behavior
      event.preventDefault();

      // Store hash
      var hash = this.hash;

      // Using jQuery's animate() method to add smooth page scroll
      // The optional number (900) specifies the number of milliseconds it takes to scroll to the specified area
      $('html, body').animate({
        scrollTop: $(hash).offset().top
      }, 900, function(){

        // Add hash (#) to URL when done scrolling (default click behavior)
        window.location.hash = hash;
      });
    } // End if
  });
})
</script>

